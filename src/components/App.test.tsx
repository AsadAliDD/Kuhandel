import { fireEvent, render, screen } from '@testing-library/react'; import { describe, expect, it } from 'vitest'; import App from '../App';
describe('representative pass-and-play flow',()=>{it('starts a game, opens an auction, bids and resolves it',()=>{localStorage.clear();render(<App/>);fireEvent.click(screen.getByRole('button',{name:/Partie starten/i}));expect(screen.getByText(/ist am Zug/)).not.toBeNull();fireEvent.click(screen.getByRole('button',{name:/Auktion eröffnen/i}));expect(screen.getByText(/Aktuelles Höchstgebot/i)).not.toBeNull();fireEvent.click(screen.getByRole('button',{name:'+10'}));fireEvent.click(screen.getByRole('button',{name:/Zuschlag erteilen/i}));expect(screen.getByText(/gewinnt .* für 10/i)).not.toBeNull();});});
