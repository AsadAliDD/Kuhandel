import { fireEvent, render, screen } from '@testing-library/react'; import { describe, expect, it } from 'vitest'; import App from '../App';
describe('representative pass-and-play flow',()=>{it('starts a game, opens an auction, bids and resolves it',()=>{localStorage.clear();render(<App/>);fireEvent.click(screen.getByRole('button',{name:/start game/i}));expect(screen.getByText(/'s turn/)).not.toBeNull();fireEvent.click(screen.getByRole('button',{name:/open auction/i}));expect(screen.getByText(/current highest bid/i)).not.toBeNull();fireEvent.click(screen.getByRole('button',{name:'+10'}));fireEvent.click(screen.getByRole('button',{name:/close auction/i}));expect(screen.getByText(/wins .* for 10/i)).not.toBeNull();});});
