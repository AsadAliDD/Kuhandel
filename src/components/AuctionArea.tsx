import { Gavel, Layers3, Sparkles } from 'lucide-react'; import { AnimatePresence, motion } from 'framer-motion'; import type { GameState } from '../game/types'; import { ANIMALS } from '../game/types'; import { Button } from './Button';
export function AuctionArea({state,send}:{state:GameState;send:(a:any)=>void}){const auction=state.auction,a=ANIMALS.find(x=>x.id===auction?.card.animal);return <section className="auction card">{auction&&a?<><div className="lot"><span className="lot-number">LOT {32-state.deck.length}</span><motion.span className="hero-animal" initial={{ opacity: 0, rotate: -12, scale: .6 }} animate={{ opacity: 1, rotate: 0, scale: 1 }} transition={{ type: 'spring', stiffness: 300, damping: 22 }}>{a.emoji}</motion.span><span className="reveal"><Sparkles size={14}/> Revealed</span><h2>{a.name}</h2><p>Set value · {a.value} points</p></div><div className="bid-panel"><span className="kicker">Current highest bid</span><div className="bid-value"><AnimatePresence mode="popLayout" initial={false}><motion.span key={auction.highestBid} initial={{ opacity: 0, y: -12 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 12 }} transition={{ duration: .16 }}>{auction.highestBid}</motion.span></AnimatePresence><small>coins</small></div><div className="quick-bids">{[10,20,50,100].map(n=><Button variant="secondary" key={n} onClick={()=>{const bidder=state.players.find((_,i)=>i!==state.currentPlayer)!;send({type:'BID',playerId:bidder.id,amount:auction.highestBid+n})}}>+{n}</Button>)}</div><Button onClick={()=>send({type:'RESOLVE_AUCTION'})}><Gavel size={18}/> Close auction</Button><p className="hint">Players take turns entering bids on this device.</p></div></>:<div className="auction-empty"><div className="deck-stack"><Layers3/><b>{state.deck.length}</b></div><span className="kicker">You are the auctioneer</span><h2>The next lot is waiting.</h2><p>Reveal an animal card and collect bids from the other players.</p><Button disabled={!state.deck.length} onClick={()=>send({type:'DRAW_AUCTION'})}><Gavel size={18}/> Open auction</Button></div>}</section>}
