import { Trophy } from 'lucide-react'; import { scorePlayer } from '../game/rules'; import type { GameState } from '../game/types'; import { Button } from './Button';
export function FinalStandings({state,onRestart}:{state:GameState;onRestart:()=>void}){const sorted=[...state.players].sort((a,b)=>scorePlayer(b)-scorePlayer(a));return <main className="final"><Trophy size={48}/><span className="kicker">Viehkönig gekürt</span><h1>{sorted[0].name} gewinnt!</h1><div className="standings card">{sorted.map((p,i)=><div key={p.id}><b>{i+1}</b><strong>{p.name}</strong><span>{scorePlayer(p)} Punkte</span></div>)}</div><Button onClick={onRestart}>Neue Partie</Button></main>}
