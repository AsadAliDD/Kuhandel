import { Eye } from 'lucide-react'; import { totalMoney } from '../game/rules'; import type { Money } from '../game/types';
export function MoneyHand({money}:{money:Money[]}){return <section className="money card"><header><div><span className="kicker">For your eyes only</span><h2>Money cards</h2></div><span className="balance"><Eye size={16}/> Total {totalMoney(money)}</span></header><div className="banknotes">{money.map((m,i)=><button key={`${m}-${i}`} className={m===0?'bluff':''}><small>Kuhhandel</small><b>{m||'Bluff'}</b><span>{m ? 'coins' : 'worthless'}</span></button>)}</div></section>}
