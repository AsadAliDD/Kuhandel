import { Eye } from 'lucide-react'; import { totalMoney } from '../game/rules'; import type { Money } from '../game/types';
export function MoneyHand({money}:{money:Money[]}){return <section className="money card"><header><div><span className="kicker">Nur für deine Augen</span><h2>Geldkarten</h2></div><span className="balance"><Eye size={16}/> Gesamt {totalMoney(money)}</span></header><div className="banknotes">{money.map((m,i)=><button key={`${m}-${i}`} className={m===0?'bluff':''}><small>Kuhhandel</small><b>{m||'Bluff'}</b><span>{m?'Taler':'wertlos'}</span></button>)}</div></section>}
