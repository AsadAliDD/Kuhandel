import { ANIMALS, type Player } from '../game/types'; import { animalCount } from '../game/rules';
export function AnimalCollection({player}:{player:Player}){return <section className="collection card"><header><div><span className="kicker">Dein Viehbestand</span><h2>Tierkollektion</h2></div><span>{player.animals.length} Karten</span></header><div className="animal-grid">{ANIMALS.map(a=>{const count=animalCount(player,a.id);return <article className={`animal-card ${count===4?'complete':''} ${!count?'empty':''}`} key={a.id}><span className="animal-emoji">{a.emoji}</span><strong>{a.name}</strong><small>{a.value} Punkte</small><div className="pips">{[0,1,2,3].map(i=><i className={i<count?'filled':''} key={i}/>)}</div>{count===4&&<b>Quartett!</b>}</article>})}</div></section>}
