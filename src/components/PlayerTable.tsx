import { Coins } from 'lucide-react'; import { totalMoney } from '../game/rules'; import type { Player } from '../game/types';
export function PlayerTable({players,current}:{players:Player[];current:number}){return <section className="players" aria-label="Spielende">{players.map((p,i)=><article className={`player ${i===current?'active':''}`} key={p.id}><div className="avatar">{p.name[0]}</div><div><strong>{p.name}</strong><small>{p.animals.length} Tiere</small></div><span className="wealth"><Coins size={14}/>{totalMoney(p.money)}</span>{i===current&&<i>am Zug</i>}</article>)}</section>}
